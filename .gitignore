# Agent worktrees — scratch clones, never part of the repo
.claude/worktrees/

# OS cruft
.DS_Store

# Build + deps
node_modules/
dist/
*.local

# The native projects (ios/, android/) ship their own .gitignore, generated by
# `cap add` — they already exclude the copied web assets and build output.
